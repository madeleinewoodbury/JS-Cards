import React from 'react';
import ListForm from './ListForm';

const Navbar = () => {
  return (
    <nav className="Navbar">
      <h2>JS Cards</h2>
      <ListForm />
    </nav>
  );
};

export default Navbar;
